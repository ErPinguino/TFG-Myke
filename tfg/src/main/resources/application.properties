spring.application.name=tfg

spring.datasource.url=jdbc:postgresql://postgres:5432/minecraft_project
spring.datasource.username=pingu
spring.datasource.password=miausita25
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.docker.compose.file=./compose.yaml

server.port=8443

server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=miPasswordSeguro
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=tomcat

server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true

logging.level.org.springframework.security=DEBUG
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

# Para que arranque el schema DDL de session en PostgreSQL:
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:org/springframework/session/jdbc/schema-postgresql.sql


